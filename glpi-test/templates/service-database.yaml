apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-database
  labels:
    app: {{ .Release.Name }}-database
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.service.databasePort }}
    targetPort: 3306
  selector:
    app: {{ .Release.Name }}-database